# Frontend Debug Configuration
# Copy this file to .env.local and customize for your development needs

# Enable comprehensive debug logging (CLAUDE.md rule #8)
# Set to 'true' to enable debug mode, 'false' to disable
VITE_DEBUG=true
VITE_DEBUG_ENABLED=true

# Debug categories to enable (comma-separated)
# Available: component, hook, api, state, event, performance, navigation, render, lifecycle
# Leave empty to enable all categories, or specify only the ones you need
VITE_DEBUG_CATEGORIES=component,hook,api,state,event

# Performance debugging (can be heavy, use sparingly)
# Set to 'true' only when debugging performance issues
VITE_DEBUG_PERFORMANCE=false

# Backend logging integration
# Set to 'false' to prevent frontend debug logs from being sent to backend
VITE_DEBUG_SEND_TO_BACKEND=true

# Console logging
# Set to 'false' to suppress console.log output from debug utility
VITE_DEBUG_CONSOLE_ENABLED=true

# API Configuration
VITE_API_URL=http://localhost:3000

# Feature flags
VITE_ENABLE_AUTH=false
VITE_ENABLE_ANALYTICS=false

# Environment
VITE_ENV=development

# Debug Examples:
# 
# For component debugging only:
# VITE_DEBUG_CATEGORIES=component,render
#
# For API debugging only:
# VITE_DEBUG_CATEGORIES=api,hook
#
# For performance debugging:
# VITE_DEBUG_CATEGORIES=performance
# VITE_DEBUG_PERFORMANCE=true
#
# For production debugging (minimal):
# VITE_DEBUG_CATEGORIES=api,event
# VITE_DEBUG_CONSOLE_ENABLED=false
# VITE_DEBUG_SEND_TO_BACKEND=true