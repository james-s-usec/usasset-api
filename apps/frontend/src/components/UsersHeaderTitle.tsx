import React from 'react';
import { Typography } from '@mui/material';

export const UsersHeaderTitle = (): React.ReactElement => {
  return (
    <Typography variant="h4" component="h1">
      User Management
    </Typography>
  );
};